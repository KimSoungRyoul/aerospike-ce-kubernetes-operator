{{- if eq (include "aerospike-ce-kubernetes-operator.operator.enabled" .) "true" }}
{{- if and .Values.webhook.enabled .Values.certManager.enabled }}
{{- if eq .Values.certManager.issuer.type "selfSigned" }}
apiVersion: cert-manager.io/v1
kind: Issuer
metadata:
  name: {{ include "aerospike-ce-kubernetes-operator.issuerName" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "aerospike-ce-kubernetes-operator.labels" . | nindent 4 }}
spec:
  selfSigned: {}
---
{{- else if eq .Values.certManager.issuer.type "ca" }}
apiVersion: cert-manager.io/v1
kind: Issuer
metadata:
  name: {{ include "aerospike-ce-kubernetes-operator.issuerName" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "aerospike-ce-kubernetes-operator.labels" . | nindent 4 }}
spec:
  ca:
    secretName: {{ .Values.certManager.issuer.caSecretName }}
---
{{- end }}
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: {{ include "aerospike-ce-kubernetes-operator.certName" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "aerospike-ce-kubernetes-operator.labels" . | nindent 4 }}
spec:
  dnsNames:
    - {{ include "aerospike-ce-kubernetes-operator.webhookServiceName" . }}.{{ .Release.Namespace }}.svc
    - {{ include "aerospike-ce-kubernetes-operator.webhookServiceName" . }}.{{ .Release.Namespace }}.svc.cluster.local
    # Metrics service SANs: the controller-manager serves /metrics on port
    # 8443 with the same serving-certs directory as the webhook. Including
    # the metrics service DNS lets ServiceMonitor verify the scrape target
    # against the cert-manager-issued CA instead of insecureSkipVerify.
    - {{ include "aerospike-ce-kubernetes-operator.metricsServiceName" . }}.{{ .Release.Namespace }}.svc
    - {{ include "aerospike-ce-kubernetes-operator.metricsServiceName" . }}.{{ .Release.Namespace }}.svc.cluster.local
  {{- if eq .Values.certManager.issuer.type "clusterIssuer" }}
  issuerRef:
    kind: ClusterIssuer
    name: {{ .Values.certManager.issuer.name }}
  {{- else }}
  issuerRef:
    kind: Issuer
    name: {{ include "aerospike-ce-kubernetes-operator.issuerName" . }}
  {{- end }}
  secretName: {{ include "aerospike-ce-kubernetes-operator.certSecretName" . }}
  privateKey:
    rotationPolicy: Always
  {{- with .Values.certManager.duration }}
  duration: {{ . }}
  {{- end }}
  {{- with .Values.certManager.renewBefore }}
  renewBefore: {{ . }}
  {{- end }}
{{- end }}
{{- end }}
